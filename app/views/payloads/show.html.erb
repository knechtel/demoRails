<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @payload.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @payload.content %>
</p>

<%= link_to 'Edit', edit_payload_path(@payload) %> |
<%= link_to 'Back', payloads_path %>
